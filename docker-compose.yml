services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-context-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - ai-context-service/.env
    volumes:
      - ${HOST_DATA_DIR:-~/data}:/data
